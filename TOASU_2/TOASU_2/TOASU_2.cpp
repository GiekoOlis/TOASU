//Алгоритм вычеркивания для определения ранга вершин
#include "pch.h"
#include <iostream>
#include <stdio.h>
#define n 5
using namespace std;
int main()
{
	int r[n], np[n], nm[n], v[n], u[n];
	for (int i = 0; i < n; i++) {
		r[i] = 0; np[i] = 0; nm[i] = 0; v[i] = -1; u[i] = NULL;
	}
	for () {
		
	}
	for (int i = 1; i < n-1; i++) {
		if (nm[i]==0 || np[i]==0) {
			cout << i << " - нетранзитивная вершина" << endl;
			exit();
		}
	}
	//-----------------------------------//
	U[0] = j[0]; K[1] = 0; 
	for (int k = 0; k < n - 1; k++) {
		j = V[k];
		if (j == -1) cout << "имеется цикл" << endl;
	}
	for () {

	}
}

